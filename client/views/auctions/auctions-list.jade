h3.text-center Auction List View
br
.panel.panel-default
  // Default panel contents
  .panel-heading.text-center Sort Listings By:
  .panel-body
    .col-xs-12.col-sm-6.col-md-3.col-lg-3
      label(for='state') Auctions
      select.form-control#states(ng-options='state as state for state in states', ng-model='user.address.state')
        option
    .col-xs-12.col-sm-6.col-md-3.col-lg-3
      label(for='state') Categories
      select.form-control#states(ng-options='state as state for state in states', ng-model='user.address.state')
        option
    .col-xs-12.col-sm-6.col-md-3.col-lg-3
      label(for='state') Current Price
      select.form-control#states(ng-options='state as state for state in states', ng-model='user.address.state')
        option
    .col-xs-12.col-sm-6.col-md-3.col-lg-3
      label(for='state') End Time
      select.form-control#states(ng-options='state as state for state in states', ng-model='user.address.state')
        option
.row#itemCard(ng-repeat='item in items track by $index')
  .col-xs-12
    .col-xs-12.col-sm-6.col-md-4.col-lg-3
      img(base-sixty-four-image='item.photo' ui-sref='auctions.show {itemId: item._id}')
    .col-xs-12.col-sm-6.col-md-4
      //{{item.title | limitTo: stringLimit}} how to limit the text length later on.
      h5: a(ui-sref='auctions.show {itemId: item._id}') {{item.itemTitle}}
      h5 Lot: {{item.lot}}
      h5 Item Description: {{item.description | limitTo: 50}}
    .col-xs-12.col-sm-6.col-md-4
      h5 Current Price: {{item.currentPrice | currency}}
      h5 Ending Time: {{item.endTime | date: 'medium'}}
    .col-xs-12.col-md-4(ng-if='!item.confirm')
      button.btn.btn-block.btn-xs.btn-success(ng-click='bidConfirm(item)') BID NOW!
    .col-xs-12.col-md-4(ng-if='item.confirm')
      button.btn.btn-block.btn-xs.btn-success(ng-click='bid(item)') SUBMIT BID!
